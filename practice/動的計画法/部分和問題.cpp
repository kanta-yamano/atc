#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main()
{
    ll n, A;
    ll a[110];
    bool dp[110][10010];

    cin >> n >> A;
    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
    }
}